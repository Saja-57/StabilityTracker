import { Bot } from "lucide-react-native";

import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function AiRoute() {
  return <RoutePlaceholder icon={Bot} showBack titleKey="navigation.askAI" />;
}
